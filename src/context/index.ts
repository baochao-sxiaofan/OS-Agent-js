// Compatibility export; long-lived Agent context is defined in agent-context.
// The task-oriented context manager remains independent of that new contract.
export type { AgentContext } from './agent-context.js';
